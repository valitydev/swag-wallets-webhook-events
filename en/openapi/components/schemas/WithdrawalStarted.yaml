description: Withdrawal start event
allOf:
  - 
    $ref: '../schemas/Event.yaml'
  - 
    type: object
    required:
      - withdrawal
    properties:
      withdrawal:
        $ref: '../schemas/Withdrawal.yaml'
